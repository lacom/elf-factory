#!/usr/bin/env node

var program = require('commander');
var fs = require('fs');
var exec = require('child_process').exec;
var open = require('open');
var request = require('request');
var util = require('../lib/utilities.js')

var _exit = process.exit;

// Re-assign process.exit because of commander
// TODO: Switch to a different command framework
process.exit = exit;

var interactionName = util.slugify(interaction);
var interactionURL

request.get(interactionURL, passport.authenticate('basic', { session: false }), function(error, request, body){



}
	var _res = res;
	var _req = req;
	var displayName = _req.body.name_display.trim().toLowerCase();
	var interactionName = slugify(displayName);





open(interactionURL);